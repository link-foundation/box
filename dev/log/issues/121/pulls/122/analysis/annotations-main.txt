===== RUN 34293699247 =====
Build and Release Docker Image | success | e77abcc | 2026-09-09T00:08:16Z
-- job 102285690450 [success] js / build-js-amd64
  [failure] .github:4617  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
-- job 102285690839 [success] js / build-js-arm64
  [failure] .github:4306  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
  [warning] .github:1372  :: The command [sudo apt-get remove -y azure-cli google-chrome-stable firefox powershell mono-devel libgl1-mesa-dri --fix-missing] failed to complete successfully. Proceeding...
-- job 102287448077 [success] essentials / build-essentials-amd64
  [failure] .github:1830  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
-- job 102287448096 [success] essentials / build-essentials-arm64
  [failure] .github:1892  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
  [warning] .github:1372  :: The command [sudo apt-get remove -y azure-cli google-chrome-stable firefox powershell mono-devel libgl1-mesa-dri --fix-missing] failed to complete successfully. Proceeding...
-- job 102289844527 [success] languages / build-languages-amd64 (kotlin)
  [failure] .github:891  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
-- job 102289844550 [success] languages / build-languages-amd64 (rust)
  [failure] .github:739  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
-- job 102289844564 [success] languages / build-languages-amd64 (ruby)
  [failure] .github:744  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
-- job 102289844572 [success] languages / build-languages-amd64 (php)
  [failure] .github:1223  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
-- job 102289844588 [success] languages / build-languages-amd64 (java)
  [failure] .github:838  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
-- job 102289844598 [success] languages / build-languages-amd64 (python)
  [failure] .github:734  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
-- job 102289844599 [success] languages / build-languages-amd64 (go)
  [failure] .github:720  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
-- job 102289844704 [success] languages / build-languages-arm64 (perl)
  [failure] .github:734  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
  [warning] .github:1372  :: The command [sudo apt-get remove -y azure-cli google-chrome-stable firefox powershell mono-devel libgl1-mesa-dri --fix-missing] failed to complete successfully. Proceeding...
-- job 102289844718 [success] languages / build-languages-arm64 (go)
  [failure] .github:722  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
  [warning] .github:1372  :: The command [sudo apt-get remove -y azure-cli google-chrome-stable firefox powershell mono-devel libgl1-mesa-dri --fix-missing] failed to complete successfully. Proceeding...
-- job 102289844719 [success] languages / build-languages-arm64 (java)
  [failure] .github:828  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
  [warning] .github:1372  :: The command [sudo apt-get remove -y azure-cli google-chrome-stable firefox powershell mono-devel libgl1-mesa-dri --fix-missing] failed to complete successfully. Proceeding...
-- job 102289844721 [success] languages / build-languages-arm64 (python)
  [failure] .github:754  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
  [warning] .github:1372  :: The command [sudo apt-get remove -y azure-cli google-chrome-stable firefox powershell mono-devel libgl1-mesa-dri --fix-missing] failed to complete successfully. Proceeding...
-- job 102289844723 [success] languages / build-languages-arm64 (kotlin)
  [failure] .github:844  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
  [warning] .github:1372  :: The command [sudo apt-get remove -y azure-cli google-chrome-stable firefox powershell mono-devel libgl1-mesa-dri --fix-missing] failed to complete successfully. Proceeding...
-- job 102289844733 [success] languages / build-languages-arm64 (swift)
  [failure] .github:701  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
  [warning] .github:1372  :: The command [sudo apt-get remove -y azure-cli google-chrome-stable firefox powershell mono-devel libgl1-mesa-dri --fix-missing] failed to complete successfully. Proceeding...
-- job 102289844739 [success] languages / build-languages-arm64 (rust)
  [failure] .github:766  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
  [warning] .github:1372  :: The command [sudo apt-get remove -y azure-cli google-chrome-stable firefox powershell mono-devel libgl1-mesa-dri --fix-missing] failed to complete successfully. Proceeding...
-- job 102289844741 [success] languages / build-languages-amd64 (lean)
  [failure] .github:719  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
-- job 102289844768 [success] languages / build-languages-amd64 (rocq)
  [failure] .github:889  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
-- job 102289844771 [success] languages / build-languages-arm64 (ruby)
  [failure] .github:730  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
  [warning] .github:1372  :: The command [sudo apt-get remove -y azure-cli google-chrome-stable firefox powershell mono-devel libgl1-mesa-dri --fix-missing] failed to complete successfully. Proceeding...
-- job 102289844774 [success] languages / build-languages-arm64 (php)
  [failure] .github:1245  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
  [warning] .github:1372  :: The command [sudo apt-get remove -y azure-cli google-chrome-stable firefox powershell mono-devel libgl1-mesa-dri --fix-missing] failed to complete successfully. Proceeding...
-- job 102289844786 [success] languages / build-languages-arm64 (lean)
  [failure] .github:716  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
  [warning] .github:1374  :: The command [sudo apt-get remove -y azure-cli google-chrome-stable firefox powershell mono-devel libgl1-mesa-dri --fix-missing] failed to complete successfully. Proceeding...
-- job 102289844823 [success] languages / build-languages-arm64 (rocq)
  [failure] .github:911  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
  [warning] .github:1374  :: The command [sudo apt-get remove -y azure-cli google-chrome-stable firefox powershell mono-devel libgl1-mesa-dri --fix-missing] failed to complete successfully. Proceeding...
-- job 102289844838 [success] languages / build-languages-amd64 (perl)
  [failure] .github:723  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
-- job 102289844868 [success] languages / build-languages-amd64 (swift)
  [failure] .github:683  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
-- job 102294023714 [success] full / docker-build-push
  [warning] .github:8  :: ERROR: failed to remove one or more builders
  [failure] .github:2067  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
-- job 102297905648 [success] full / docker-build-push-arm64
  [failure] .github:2152  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
  [warning] .github:1372  :: The command [sudo apt-get remove -y azure-cli google-chrome-stable firefox powershell mono-devel libgl1-mesa-dri --fix-missing] failed to complete successfully. Proceeding...
-- job 102304409659 [success] dind / build-dind-amd64 (rust)
  [failure] .github:1077  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
-- job 102304409671 [success] dind / build-dind-amd64 (java)
  [failure] .github:1024  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
-- job 102304409676 [success] dind / build-dind-amd64 (essentials)
  [failure] .github:1023  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
-- job 102304409689 [success] dind / build-dind-amd64 (php)
  [failure] .github:1045  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
-- job 102304409693 [success] dind / build-dind-amd64 (full)
  [failure] .github:1292  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
-- job 102304409695 [success] dind / build-dind-amd64 (ruby)
  [failure] .github:999  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
-- job 102304409696 [success] dind / build-dind-amd64 (python)
  [failure] .github:1018  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
-- job 102304409712 [success] dind / build-dind-amd64 (js)
  [failure] .github:979  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
-- job 102304409718 [success] dind / build-dind-arm64 (js)
  [failure] .github:1052  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
  [warning] .github:1372  :: The command [sudo apt-get remove -y azure-cli google-chrome-stable firefox powershell mono-devel libgl1-mesa-dri --fix-missing] failed to complete successfully. Proceeding...
-- job 102304409722 [success] dind / build-dind-amd64 (kotlin)
  [failure] .github:1021  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
-- job 102304409726 [success] dind / build-dind-amd64 (perl)
  [failure] .github:1038  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
-- job 102304409730 [success] dind / build-dind-amd64 (lean)
  [failure] .github:1057  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
-- job 102304409740 [success] dind / build-dind-amd64 (swift)
  [failure] .github:1070  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
-- job 102304409741 [success] dind / build-dind-amd64 (go)
  [failure] .github:1042  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
-- job 102304409747 [success] dind / build-dind-arm64 (essentials)
  [failure] .github:1005  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
  [warning] .github:1372  :: The command [sudo apt-get remove -y azure-cli google-chrome-stable firefox powershell mono-devel libgl1-mesa-dri --fix-missing] failed to complete successfully. Proceeding...
-- job 102304409763 [success] dind / build-dind-arm64 (go)
  [failure] .github:1045  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
  [warning] .github:1372  :: The command [sudo apt-get remove -y azure-cli google-chrome-stable firefox powershell mono-devel libgl1-mesa-dri --fix-missing] failed to complete successfully. Proceeding...
-- job 102304409768 [success] dind / build-dind-amd64 (rocq)
  [failure] .github:1061  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
-- job 102304409791 [success] dind / build-dind-arm64 (python)
  [failure] .github:1010  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
  [warning] .github:1372  :: The command [sudo apt-get remove -y azure-cli google-chrome-stable firefox powershell mono-devel libgl1-mesa-dri --fix-missing] failed to complete successfully. Proceeding...
-- job 102304409828 [success] dind / build-dind-arm64 (rocq)
  [failure] .github:1040  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
  [warning] .github:1372  :: The command [sudo apt-get remove -y azure-cli google-chrome-stable firefox powershell mono-devel libgl1-mesa-dri --fix-missing] failed to complete successfully. Proceeding...
-- job 102304409833 [success] dind / build-dind-arm64 (rust)
  [failure] .github:1029  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
  [warning] .github:1374  :: The command [sudo apt-get remove -y azure-cli google-chrome-stable firefox powershell mono-devel libgl1-mesa-dri --fix-missing] failed to complete successfully. Proceeding...
-- job 102304409834 [success] dind / build-dind-arm64 (swift)
  [failure] .github:1014  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
  [warning] .github:1372  :: The command [sudo apt-get remove -y azure-cli google-chrome-stable firefox powershell mono-devel libgl1-mesa-dri --fix-missing] failed to complete successfully. Proceeding...
-- job 102304409849 [success] dind / build-dind-arm64 (lean)
  [failure] .github:1048  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
  [warning] .github:1372  :: The command [sudo apt-get remove -y azure-cli google-chrome-stable firefox powershell mono-devel libgl1-mesa-dri --fix-missing] failed to complete successfully. Proceeding...
-- job 102304409851 [success] dind / build-dind-arm64 (kotlin)
  [failure] .github:1027  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
  [warning] .github:1372  :: The command [sudo apt-get remove -y azure-cli google-chrome-stable firefox powershell mono-devel libgl1-mesa-dri --fix-missing] failed to complete successfully. Proceeding...
-- job 102304409854 [success] dind / build-dind-arm64 (full)
  [failure] .github:1327  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
  [warning] .github:1374  :: The command [sudo apt-get remove -y azure-cli google-chrome-stable firefox powershell mono-devel libgl1-mesa-dri --fix-missing] failed to complete successfully. Proceeding...
-- job 102304409855 [success] dind / build-dind-arm64 (ruby)
  [failure] .github:1013  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
  [warning] .github:1372  :: The command [sudo apt-get remove -y azure-cli google-chrome-stable firefox powershell mono-devel libgl1-mesa-dri --fix-missing] failed to complete successfully. Proceeding...
-- job 102304409858 [success] dind / build-dind-arm64 (php)
  [failure] .github:1103  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
  [warning] .github:1372  :: The command [sudo apt-get remove -y azure-cli google-chrome-stable firefox powershell mono-devel libgl1-mesa-dri --fix-missing] failed to complete successfully. Proceeding...
-- job 102304409892 [success] dind / build-dind-arm64 (perl)
  [failure] .github:1015  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
  [warning] .github:1372  :: The command [sudo apt-get remove -y azure-cli google-chrome-stable firefox powershell mono-devel libgl1-mesa-dri --fix-missing] failed to complete successfully. Proceeding...
-- job 102304409905 [success] dind / build-dind-arm64 (java)
  [failure] .github:1035  :: Process completed with exit code 143.\n  ##[error]The runner has received a shutdown signal.\n\n11 MB of disk consumed across the whole 4.5-minute export with 88 GB still\nfree, against 2.3 GB -> 18.8 GB of committed memory still climbing at ~2 GB\nper 30s when the runner was taken down. `dind-full` recorded the identical\ncurve 22 minutes earlier. So every reclaim on this branch so far was aimed at\nthe wrong resource: these jobs run out of memory, not disk.\n\ndockerd buffering an export in anonymous memory is docker/buildx#1606, open\nagainst the docker driver since Docker 23.0, with no daemon setting to turn it\noff. What changed on our side is what gets exported: the same two jobs passed\non 2026-09-05 (run 33962512941, `exporting layers 171.1s done`) on the same\nrunner image and Docker 28.0.4, and commit 46f80a5 then made the Lean boxes\ninstall a real toolchain, so the full box's `COPY --from=lean-stage\n/home/box/.elan` stopped copying a stub.\n\n16 GB of RAM is what an ubuntu-24.04 runner has and larger runners are not\navailable here, so scripts/ci/ensure-swap.sh converts the disk the measurement\nshows sitting idle into the resource that ran out: a 32 GB total swap target,\n29697 MB of it as a swapfile on /, sized down from the target only if it would\nencroach on the 40 GB reserve the chain and its export still need. It decides\nout loud on `[ensure-swap]` lines, `plan` mode prints the decision without\ntouching anything, `sudo -n` means a machine that would prompt fails instantly\ninstead of hanging, and failing to provision is a warning rather than a job\nfailure - the failure belongs on the build, whose log now explains itself.\n\npr-test-dind's `full` variant gets 90 minutes instead of 60: it reaches the\nexport ~37 minutes in and now spends that export paging, and a build killed\nwhile it is making progress reports a timeout instead of the fix.\n\nThe sampler also names the biggest processes by resident memory now, because\n\"memory ran out\" and \"this process took it\" are different findings and only\nthe second one points at anything - the daemon doing the export runs outside\nthe step's own process tree.\n\nexperiments/test-issue119-ci-resource-headroom.sh grows from 14 to 40\nassertions: the sizing arithmetic and the reserve guard through `plan` mode,\nand the sequence - allocate, format, enable, retry with dd when swapon refuses\na preallocated file, clean up what could not be enabled, recognise its own\nswapfile on a second call - against stubs, because provisioning needs root and\na test that needs root is a test that does not run. Verified to fail on the\npre-fix tree in a scratch worktree, both for the missing script and for the\nmissing wiring.",
  [warning] .github:1372  :: The command [sudo apt-get remove -y azure-cli google-chrome-stable firefox powershell mono-devel libgl1-mesa-dri --fix-missing] failed to complete successfully. Proceeding...
===== RUN 34293699033 =====
Workflows | success | e77abcc | 2026-09-09T00:08:16Z
===== RUN 34293699154 =====
Security | success | e77abcc | 2026-09-09T00:08:16Z
===== RUN 34293699072 =====
Scripts | failure | e77abcc | 2026-09-09T00:08:16Z
-- job 102285576328 [failure] scripts / regression suites
  [failure] .github:103  :: Process completed with exit code 1.
  [failure] experiments/test-issue119-image-tags.sh:0  :: Suite failed with exit status 1
===== RUN 34293699000 =====
File sizes | success | e77abcc | 2026-09-09T00:08:15Z
===== RUN 34293698989 =====
Links | success | e77abcc | 2026-09-09T00:08:15Z
-- job 102285575681 [success] links / lychee
  [notice] .github:135  :: Summary report available at: https://github.com/link-foundation/box/actions/runs/34293698989#summary-102285575681
===== RUN 34011750117 =====
Measure Disk Space and Update README | success | b245dd8 | 2026-09-06T04:32:16Z
===== RUN 34011750123 =====
Dockerfiles | success | b245dd8 | 2026-09-06T04:32:16Z
-- job 101428679296 [success] dockerfiles / hadolint
  [notice] ubuntu/24.04/rocq/Dockerfile:11  :: DL3015 info: Avoid additional packages by specifying `--no-install-recommends`
  [notice] ubuntu/24.04/php/Dockerfile:47  :: DL3015 info: Avoid additional packages by specifying `--no-install-recommends`
  [notice] ubuntu/24.04/php/Dockerfile:43  :: DL3059 info: Multiple consecutive `RUN` instructions. Consider consolidation.
  [notice] ubuntu/24.04/js/Dockerfile:27  :: DL3015 info: Avoid additional packages by specifying `--no-install-recommends`
  [notice] ubuntu/24.04/js/Dockerfile:17  :: DL3015 info: Avoid additional packages by specifying `--no-install-recommends`
  [notice] ubuntu/24.04/full-box/Dockerfile:126  :: DL3015 info: Avoid additional packages by specifying `--no-install-recommends`
  [notice] ubuntu/24.04/full-box/Dockerfile:71  :: DL3015 info: Avoid additional packages by specifying `--no-install-recommends`
  [notice] ubuntu/24.04/essentials-box/Dockerfile:32  :: DL3015 info: Avoid additional packages by specifying `--no-install-recommends`
  [notice] Dockerfile:107  :: DL3015 info: Avoid additional packages by specifying `--no-install-recommends`
  [notice] Dockerfile:62  :: DL3015 info: Avoid additional packages by specifying `--no-install-recommends`
